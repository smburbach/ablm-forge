# ESM-C 6B (EvolutionaryScale Cambrian) exact dimensions.
# Architecture comes from configs/model/base.yaml; this preset only pins size.
model:
  hidden_size: 2560
  num_hidden_layers: 80
  num_attention_heads: 40
  max_position_embeddings: 2048
